version: 0.0
os: linux
files:
  - source: callableapis-webapp-1.0.0.war
    destination: /var/lib/tomcat/webapps/ROOT.war
hooks:
  ApplicationStart:
    - location: scripts/restart-tomcat.sh
      timeout: 300
      runas: root
